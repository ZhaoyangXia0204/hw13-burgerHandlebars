<h1 class="nav has-text-centered has-text-danger is-size-3">Burgers!</h1>
<div class="columns has-text-centered has-text-danger is-size-3">
<div class="column" >
<h2>Burgers have been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

</div>

<div class="column">
<h2>Burgers still there!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

</div>
</div>


<div class="has-text-centered has-text-danger is-size-3">
  <h3>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bur">burger Name:</label>
    <input type="text" id="bur" name="burger_name">
  </div>

  <button class="button is-warning is-light" type="submit">Add Burger</button>
</form>


</div>
